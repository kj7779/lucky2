{% extends 'base.html.twig' %}

{% block title %}Diary3{% endblock %}

{% block body %}
    <h1>Diary3</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ diary3.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ diary3.date ? diary3.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ diary3.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_diary3_index') }}">back to list</a>

    <a href="{{ path('app_diary3_edit', {'id': diary3.id}) }}">edit</a>

    {{ include('diary3/_delete_form.html.twig') }}
{% endblock %}
